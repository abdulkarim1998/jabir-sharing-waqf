# Organization Requests Admin Dashboard

Simple admin dashboard to manage organization registration requests.

## Features

- View all pending organization requests
- Approve or reject requests
- Submit new organization requests
- Upload multiple documents per request

## Setup

```bash
npm install
npm run dev
```

The app will run on http://localhost:3001

## API

Make sure the backend API is running on http://localhost:8081
